services:
  streamlit:
    build:
      context: ..
      dockerfile: infrastructure/Dockerfile
    ports:
      - "8501:8501"
    volumes:
      - commerceos_data:/app/data
    env_file:
      - ../.env

volumes:
  commerceos_data:
